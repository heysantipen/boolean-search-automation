# Job Search Automation — Required Environment Variables
# Copy this file to .env and fill in your credentials.
# Never commit the filled-in .env to version control.

# Anthropic API — for Claude scoring
# Get yours at: https://console.anthropic.com/settings/keys
export ANTHROPIC_API_KEY='sk-ant-...'

# Tavily Search API — for Boolean ATS search
# Free tier: 1,000 credits/month at https://app.tavily.com
export BOOLEAN_TAVILY_API_KEY='tvly-...'

# Slack Incoming Webhook — for job alerts (optional)
# Create at: https://api.slack.com/apps → Incoming Webhooks
# Alerts fire for jobs scoring >= 6.5 (configurable in scripts/daily-job-check.sh)
export SLACK_WEBHOOK_URL='https://hooks.slack.com/services/...'
